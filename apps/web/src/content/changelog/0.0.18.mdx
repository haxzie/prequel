export const date = "2026-09-11";

- The editor has an Add Zoom button beside Split and Delete, and Z on the keyboard does the same. It adds a zoom where the playhead is — or, when one is already there, in the nearest free stretch beside it — and selects it, so the next thing you do is adjust it.
- Hovering the zoom row shows the zoom a click will add, labelled Add Zoom, rather than an unmarked outline.
- Resting the pointer on an icon in the dock, or on the editor's side rail, shows what it does. The first label waits a moment; after that they follow the pointer from icon to icon.
