export const date = "2026-09-25";
export const highlight = "Dream, and four retro palettes";

- A new Dream filter, soft and hazy over the whole picture — as mist, as a halo, or at the edges only.
- Pico-8, Game Boy, C64 and Risograph each take the picture down to that machine's colours.
- A new Dither filter takes it down to two colours on a coarse grid.
- A new Lost signal filter: soft towards the edges, blooming, and off colour.
- Pixelate is now five styles of its own in the grid. A clip already set to it needs one picking again.
